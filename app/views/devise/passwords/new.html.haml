.text-center
  %h1 Forgot Your Password?
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'form-auth text-left' }) do |form|
    = form_errors_for(resource)
    .form-row.mb-4
      .col
        = form.label :email, class: 'sr-only'
        = form.email_field :email, autofocus: true, autocomplete: 'email', placeholder: 'Email', class: 'form-control'
    = form.submit "Send me reset password instructions", class: 'btn btn-lg btn-block'
  .mt-4= render 'devise/shared/links'
